export type ApiConfiguration = {
  change_keys: string[],
  images: {
    base_url: string,
    secure_base_url: string,
    backdrop_sizer: string[],
    logo_sizes: string[],
    poster_sizes: string[],
    profile_sizes: string[],
    still_sizes: string[],
  }
}
